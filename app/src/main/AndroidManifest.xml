<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.frecycle">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SettingsActivity"></activity>
        <activity android:name=".ShowProfilePhotoActivity" />
        <activity android:name=".ProfileActivity" android:noHistory="true"/>
        <activity android:name=".SignInUpActivity" android:parentActivityName=".AccountActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".AccountActivity"/>

        </activity>
        <activity android:name=".AccountActivity" android:noHistory="true" />
        <activity android:name=".FavoritesActivity" android:noHistory="true" />
        <activity android:name=".SearchActivity" android:noHistory="true" />
        <activity android:name=".HomeActivity" android:noHistory="true" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>